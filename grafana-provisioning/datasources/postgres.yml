apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: postgres
    url: postgres:5432
    database: logly_db
    user: logly
    secureJsonData:
      password: logly_password
    jsonData:
      sslmode: disable
    editable: true